# ================================================================
# NEUROSENTINEL LITE - PYTHON 3.12 COMPATIBLE
# Render.com Production Build
# ================================================================

# ── CORE FRAMEWORK ──────────────────────────────────────────────
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# ── PYTORCH (must install BEFORE torch-geometric/scatter) ───────
# CPU-only version for Render free tier (no GPU)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.1+cpu
numpy==1.26.4

# ── DATA & ML ───────────────────────────────────────────────────
pandas==2.2.2
scikit-learn==1.5.0

# ── GNN (install AFTER torch) ───────────────────────────────────
# torch-scatter and torch-sparse removed — torch-geometric handles them
torch-geometric==2.4.0
networkx==3.3
plotly==5.24.1

# ── EMBEDDINGS & LLM ────────────────────────────────────────────
requests==2.31.0
redis==5.0.1
sentence-transformers==2.2.2

# ── SERVER & UTILITIES ──────────────────────────────────────────
python-dotenv==1.0.0
click==8.1.7

# ── KAFKA (mock mode — lightweight client only) ─────────────────
# confluent-kafka REMOVED (requires C compiler, fails on Render)
kafka-python==2.0.2

# ── DATABASE ────────────────────────────────────────────────────
psycopg2-binary==2.9.9
sqlalchemy==2.0.32
alembic==1.13.3

# ── MONITORING ──────────────────────────────────────────────────
prometheus-client==0.20.0
python-json-logger==2.0.7

# ── TESTING ─────────────────────────────────────────────────────
pytest==8.3.2
pytest-asyncio==0.24.0
httpx==0.27.2